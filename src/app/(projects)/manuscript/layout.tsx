import { LayoutProps } from '@/types/types'
import React from 'react'

const ManuscriptLayout = ({children}: LayoutProps) => {
  return (
    <div>{children}</div>
  )
}

export default ManuscriptLayout